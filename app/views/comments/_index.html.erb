<ul>
  <% comments.each do |comment| %>
    <% if comment.id.present? %>
      <li>
      <!-- Addendum -->
        <div class="comment-content" id ="this_comment_<%= comment.id %>"></div>
      <!-- Jusqu'à présent -->
        <p>Commentaire : <%= comment.content %></p>
        <!-- Addendum -->
        <p>Client : <%= comment.client.name %></p>
        <p><%= link_to "Éditer Commentaire", edit_order_comment_path(order, comment), remote: true %></p>
        <!-- Jusqu'à présent -->
        <p><%= link_to "Supprimer Commentaire", order_comment_path(comment.order_id, comment.id), method: :delete, remote: true,
                                        data: { confirm: "Voulez-vous vraiment supprimer cela?"} %></p>
        <!-- Jusqu'à présent -->
      </li>
    <% end %>
  <% end %>
</ul>


